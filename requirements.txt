# ETF MANAGER -- 실행 시점(2026-09)에 설치·검증된 버전
# 상한(<)은 호환성 안전용. 필요 시 조정하세요.
streamlit>=1.63,<2
pandas>=2.2,<4
numpy>=1.26,<3
yfinance>=1.6,<2
finance-datareader>=0.9.202,<1
# 알파카 시세 / 방문자 집계에서 직접 씁니다. 지금은 streamlit 이 딸려 오지만,
# 남의 패키지에 얹혀 가면 그쪽이 빼는 순간 앱이 안 켜집니다. 직접 적어 둡니다.
requests>=2.31,<3

# 개발/테스트용
pytest>=8,<10
