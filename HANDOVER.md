# ETF MANAGER 2027 — 유지보수 인수인계서

이 문서 하나만 읽으면 새 대화(또는 새 개발자)가 바로 유지보수를 이어갈 수 있도록 정리한 문서입니다.
**작업을 시작하기 전에 이 문서를 끝까지 읽으세요.** 특히 "8. 반드시 알아야 할 함정들"은
모르고 건드리면 반드시 같은 실수를 반복하게 되는 내용입니다.

- 최종 버전: `config.APP_VERSION` 참고 (이 문서 작성 시점 **v1.0.5**)
- 실행: `streamlit run app.py`
- 테스트: `pytest -q --ignore=tests/test_network` (네트워크 포함: `pytest -m network`)

---

## 1. 이 프로그램이 뭔가

주식/ETF **배당(분배금) 포트폴리오를 짜고 검증하는 도구**입니다.
축구 전술판(FM 스타일) 위에 종목 카드를 배치하면서 비중을 정하면,
실제 시세·환율·분배금 데이터를 불러와 **몇 주를 살 수 있고, 매달 얼마를 받게 되는지**
계산해 줍니다. 과거 시점부터 그냥 사서 들고 있었다면 어땠을지(Buy & Hold) 백테스트도 됩니다.

주 사용자는 **배당/분배금 포트폴리오를 짜는 개인 투자자**입니다.

### 절대 어기면 안 되는 원칙 (프로젝트 최상위 규칙)

1. **데이터를 지어내지 않는다.** 값이 없으면 "데이터 없음"이라고 표시하지, 추정치를 슬쩍 넣지 않습니다.
   추정을 해야 할 때는 반드시 추정이라고 화면에 밝히고 근거(지급 횟수 등)를 같이 보여줍니다.
2. **투자 추천을 하지 않는다.** 점수·등급·"좋은 종목" 같은 주관적 평가를 넣지 않습니다.
   예시 포트폴리오(`presets.py`)도 "추천"이 아니라 "예시"이며, 분류 기준도 우열이 아니라
   **분배금이 나오는 방식**(배당/임대료/이자 vs 커버드콜 옵션 프리미엄)이라는 사실 구분입니다.
3. **계산 로직과 화면을 섞지 않는다.** `app.py`는 화면 조립만, 계산은 `services/`, 데이터는 `data/providers/`.
4. **바꿀 수 있는 값은 `config.py` 한 곳에.** 같은 값을 여러 파일에 하드코딩하지 않습니다.

---

## 2. 파일 지도 (어디를 고쳐야 하나)

| 하고 싶은 일 | 고칠 파일 |
|---|---|
| 연도·버전·기본값·전술판 비율 변경 | `config.py` |
| 화면 글꼴/글자색/크기/요약 박스 스타일 | `ui_theme.py` |
| 화면 구성·문구·버튼 배치 | `app.py` |
| 전술판(축구장) 모양·드래그·캡처 | `components/football_pitch/frontend/index.html` |
| 금액/수량 입력 카드 | `components/buy_input/frontend/index.html` |
| 예시 포트폴리오 종목 | `presets.py` |
| 매수수량·분배금·수익률 계산식 | `services/calculation_service.py` |
| 종목별 계산 조립 | `services/portfolio_service.py` |
| 분배금(TTM·연환산 추정) | `services/distribution_service.py` |
| 백테스트 | `services/backtest_service.py` |
| 환율 | `services/fx_service.py` |
| 저장/불러오기(JSON) | `services/tactic_service.py` |
| 데이터 소스(yfinance·FDR) | `data/providers/` |
| 전술판 슬롯 좌표 | `pitch_grid.py` |
| 유니폼 색(운용사 브랜드) · 긴 ETF 이름 줄이기 | `pitch_kit.py` |

### 계층 구조

```
app.py  (Streamlit 화면. 계산 안 함)
   │
   ├── components/         커스텀 컴포넌트 (HTML/JS, 빌드 불필요)
   │     football_pitch/     전술판
   │     buy_input/          금액·수량 입력 카드
   │
   ├── services/           순수 계산 (Streamlit 의존 없음 → 테스트 쉬움)
   │
   └── data/providers/     외부 데이터 (yfinance=미국, FinanceDataReader=한국)
         registry.py         시장(US/KR) → provider 매핑
         base.py             MarketDataProvider 인터페이스, DataUnavailable
```

**데이터 소스를 바꾸려면** `data/providers/base.py`의 인터페이스를 구현한 새 provider를 만들고
`registry.py`에서 갈아끼우면 됩니다. 그 위 계층은 손댈 필요 없습니다.

---

## 3. 화면 용어 (사용자가 직접 고른 표현 — 함부로 바꾸지 말 것)

주린이도 이해할 수 있게 금융 용어를 걷어낸 표현들입니다. **여러 차례 논의해서 확정한 것이라,
새로 문구를 만들 때도 이 톤을 따르세요** (질문형·평이한 말).

| 내부 개념 | 화면 표기 |
|---|---|
| 초기자본 | **내 시드** |
| 목표비중 (target_weight) | **살(BUY) 비율** ← 슬라이더·표·경고문 등 **모든 곳에서 이 한 단어로 통일** |
| 목표금액 | 이 종목에 넣을 돈 |
| 실제 투자금 (종목별) | 이 종목에 들어간 돈 / (표에서는) **종목 원금** |
| 실제 투자금 (전체 합계) | **총 원금** |
| 실제비중 | 실제로 들어간 비율 |
| 보유수량 | 보유 주식 수 |
| 잔여현금 | **잔여현금** |
| 월/연 예상 분배금 | 한 달에 받을 분배금 / 연 예상 분배금 (요약 박스에서는 "월 분배금/연 분배금") |
| 예상 현금수익률 | 투자금 대비 예상 수익 (요약 박스에서는 "예상 수익") |
| 소수점 매수 | 소수점까지 사기 |
| 초기자본 초과 허용 | 시드보다 더 담기 허용 |
| 보유 종목 | 내가 담은 종목 |
| 선택 종목 상세 | 이 종목 자세히 보기 |
| 매수 계획 | 어떻게 살까? |
| 백테스트 | 예전부터 해봤다면? (그냥 사서 계속 갖기) |
| 매수 시점 환율 / 평가 시점 환율 | **살 때 환율 / 지금 환율** |
| 금액·수량 직접 입력 | 얼마어치 살까? (금액으로 설정) / 몇 주 살까? (수량으로 설정) |

- "분배금"과 "배당금"은 **"분배금"으로 통일**합니다(ETF 공식 용어이며 코드 전반이 이 단어를 씁니다).

---

## 4. 버전 규칙

**아무리 사소한 변경이라도 바꿀 때마다 `config.APP_VERSION`의 맨 뒷자리를 1 올립니다.**
(1.0.5 → 1.0.6 → 1.0.7 …) 화면 맨 위 제목 옆에 표시됩니다. 사용자가 명시적으로 요구한 규칙입니다.

---

## 5. 화면 구조와 실행 순서 (가장 중요)

```
헤더: 제목+버전 / 전술명 + 예시버튼 4개 / 내 시드 / 토글 3개
요약 박스 (.stat-strip)        ← 자리만 잡아두고 compute() 후에 채움
본문 3단: [좌] 담은 종목·검색 | [중] 전술판 | [우] 종목 상세
표: 어떻게 살까? (합계 줄 포함)
포트폴리오 요약 (상세) / 저장·불러오기 / 백테스트
```

### Phase A / Phase B 패턴 (반드시 지킬 것)

Streamlit은 위에서 아래로 한 번 실행됩니다. 그래서 **값을 바꾸는 위젯은 `compute()` 보다
먼저** 실행되어야 그 변경이 이번 화면에 반영됩니다. 오른쪽 컬럼을 두 번에 나눠 씁니다.

```python
with col_right:      # Phase A: 편집 위젯 (슬라이더, 입력 카드)  ← compute() 앞
    ...
comp = portfolio_service.compute(P)      # 계산
with col_right:      # Phase B: 계산 결과 표시 (지표, 경고)      ← compute() 뒤
    ...
```

`with col_right:`를 두 번 써도 화면에는 순서대로 이어 붙습니다. **이 순서를 깨면
"슬라이더를 움직였는데 숫자가 한 박자 늦게 바뀌는" 버그가 생깁니다.**

---

## 6. 커스텀 컴포넌트 두 개

빌드 도구 없이 정적 HTML+JS로 만들었습니다(`declare_component`). npm/webpack 불필요.

### football_pitch (전술판)
- 세로형 FM 스타일. 아래가 우리 진영(GK), 위가 공격 방향.
- 슬롯 격자는 `pitch_grid.py`가 정의. 카드를 끌어다 놓으면 스냅되고, 이미 있으면 서로 자리 교환.
- **카드 = 축구 유니폼 + 이름표** (`pitch_kit.py` 가 색과 이름을 정해 내려줍니다)
  - 유니폼 색 = 운용사 브랜드. 한국 종목은 목 아래 한가운데에 **태극**(원형).
    미국 종목은 흰 바탕 + 빨간 소매 + 가슴 **성조기(줄 13개, 진짜 비율)**.
    처음엔 "이 크기면 줄이 1px 도 안 되니 뭉갠다"고 보고 7~9줄로 줄였는데, 실제로
    1:1 로 찍어보니 **13줄이 제일 또렷했습니다**(안티앨리어싱이 처리해 줌).
    ⚠️ 태극기의 4괘와 성조기의 별 50개는 진짜로 뭉개져서 안 씁니다 — 태극은 원형만,
    별은 6x5 로 줄였습니다. 도형은 `starPath` / `taegeukRedPath` 한 곳에서 만들어
    화면 SVG 와 캡처 canvas 가 같은 좌표를 씁니다(둘이 어긋나면 바로 티가 납니다).
  - **등번호 = 살(BUY) 비율**. 반드시 `%` 를 같이 찍습니다 — 안 그러면 축구를 모르는
    사람은 그냥 등번호로 읽습니다(사용자 지적으로 추가됨).
  - 이름은 유니폼 **밖 이름표**에 씁니다. 유니폼 안에 넣으면 어깨·소매가 글씨 공간을
    절반(52%)이나 먹어서 한글이 7자 → 3자로 줄어듭니다.
- 📸 캡처 버튼: 외부 라이브러리 없이 `<canvas>`에 다시 그려 클립보드로 복사.
  전술판만이 아니라 **요약 띠 + 고지 + 종목 명단 + 주소**까지 함께 굽습니다
  (`summary` / `legend` / `footer` prop. 화면에는 안 나오고 캡처에만 들어감).
  카드 이름은 줄여 쓰므로, 정식 명칭은 이 명단이 보증합니다.
- 반환: `{assignments, selected_id, nonce}`

### buy_input (금액·수량 입력 카드)
- **왜 커스텀인가**: Streamlit 기본 입력칸은 엔터/포커스아웃 전에는 값을 안 넘겨줍니다.
  "타이핑하는 동안 즉시 계산"을 하려면 이 방법밖에 없습니다.
- 미리보기 계산은 **브라우저 안에서** 즉시 수행(서버 왕복 없음), 실제 값은 0.45초 디바운스 후 전송.
- 반환: `{source: "amount"|"qty", amount, qty, nonce}`

---

## 7. 테스트 전략

| 종류 | 실행 | 용도 |
|---|---|---|
| 오프라인 (기본) | `pytest -q --ignore=tests/test_network` | 항상 통과해야 함 (137개) |
| 네트워크 | `pytest -m network` | 실제 yfinance/FDR 호출 (15개). 소스 장애 시 skip |

- **`market` 픽스처** (`tests/conftest.py`): 가짜 provider를 심어 네트워크 없이 테스트.
  `market.set_us({...})`, `market.set_kr({...})`, `market.set_fx(rate=...)`.
- **`AppTest`**: `app.py`를 실제로 실행해 위젯을 조작합니다. 브라우저 자동화보다 훨씬 안정적이라
  **UI 동작 검증의 기본 수단**으로 씁니다. `market` 픽스처와 함께 쓰면 완전 오프라인.
- 종목 검색 UI는 실시간 상장목록을 쓰므로, 오프라인 테스트에서는
  `at.session_state["portfolio"] = ...`로 **Portfolio를 직접 주입**합니다.
- 커스텀 컴포넌트는 AppTest로 타이핑을 재현할 수 없어서,
  **컴포넌트가 서버로 보내는 값을 `at.session_state["buy_<id>"]`에 직접 넣어** 같은 경로를 태웁니다.

---

## 8. 반드시 알아야 할 함정들 (모르면 100% 재발)

1. **분할(액면분할) 이중 반영 금지**
   yfinance의 `Close`와 `.dividends`는 **이미 분할이 소급 반영된** 값입니다.
   백테스트 수량에 분할 배수를 또 곱하면 안 됩니다. (`splits_in_period`는 참고 표시용일 뿐)

2. **위젯 생성 후에는 그 위젯의 session_state를 바꿀 수 없음**
   `st.session_state[slider_key] = x`를 **슬라이더를 만든 뒤에** 하면
   `StreamlitWidgetAlreadyInstantiatedError`가 납니다.
   그래서 입력 카드가 보낸 값은 **슬라이더를 만들기 전에** 반영합니다.
   (`on_change` 콜백은 렌더 전에 실행되므로 예외)

3. **컴포넌트에 넘기는 값이 직전과 같으면 Streamlit이 다시 그리지 않음**
   서버가 값을 보정했는데 그 결과가 직전과 같으면 화면이 갱신되지 않습니다.
   그래서 입력할 때마다 바뀌는 `echo`(nonce)를 함께 넘겨 강제로 다시 그리게 합니다.

4. **컴포넌트가 보낸 값은 nonce로 중복 적용을 막을 것**
   안 그러면 매 렌더마다 같은 입력이 다시 적용됩니다.

5. **지운 종목이 되살아나는 문제**
   전술판 컴포넌트는 마지막에 클릭한 종목 id를 계속 들고 있습니다.
   그 id를 쓸 때는 반드시 `P.get(sid) is not None`으로 아직 존재하는지 확인하세요.

6. **PowerShell로 한글 파일을 텍스트 가공하지 말 것**
   `Get-Content -Raw | -replace | Set-Content` 같은 파이프라인은 **한글을 깨뜨립니다.**
   한글이 든 파일은 반드시 Write/Edit 도구로 직접 수정하세요.

7. **금액 계산은 반드시 `money()`를 거칠 것**
   부동소수점 오차(`10_000_000 * 0.30 != 3_000_000`) 때문에 `calculation_service.money()`로
   소수점 2자리 반올림합니다.

8. **수량 → 비중 역산에는 0.5원 여유값이 필요**
   반올림 방향 때문에 입력한 수량보다 1주 적게 나오는 경계가 있습니다.
   `(수량 × 가격 + 0.5) / 시드 × 100` 공식을 유지하세요.

9. **한국 종목코드는 숫자만이 아님**
   `0219E0`처럼 영문이 섞인 코드가 있습니다. `isdigit()`으로 거르면 데이터가 안 나옵니다.
   (`kr_provider._yf_symbols`는 `len(t) == 6`으로 판정)

10. **미리보기 서버를 재시작하면 열려 있던 브라우저 탭은 죽습니다**
    클릭해도 아무 반응이 없으면 버그가 아니라 연결이 끊긴 것 — 새로고침(F5)하면 됩니다.

11. **이미 import된 모듈은 Streamlit이 핫리로드하지 않음** ⚠️ 배포에서도 터집니다
    `components/`, `services/`, `config.py` 등을 고쳤으면 **서버를 재시작**해야 반영됩니다.
    (`app.py` 자체는 저장하면 자동 반영)

    **이건 로컬만의 문제가 아닙니다.** 배포 후 실제로 앱이 죽었습니다:
    `app.py` 는 새 코드인데 `config.py`/`services/*` 는 메모리에 남은 옛 코드가 쓰여서
    `AttributeError: comp.income_yield_on_invested_pct` 가 났고, 화면 버전은 v1.0.8,
    오류 난 줄은 v1.0.9 였습니다(뒤죽박죽 상태).

    → **푸시한 뒤 Streamlit Cloud 에서 `Manage app` → `Reboot app` 을 누르세요.**
    특히 **dataclass 에 필드를 추가**했을 때 100% 재발합니다. DEPLOY.md 참고.

12. **캡처와 화면의 글꼴 목록이 다르면, 화면에선 멀쩡한 글씨가 캡처에서만 잘린다.**
    `<canvas>` 캡처는 CSS 를 안 쓰고 `ctx.font` 에 직접 글꼴을 적습니다. 예전에 캡처 쪽만
    `"Segoe UI"` 가 빠져 있어서 영문·숫자가 맑은 고딕으로 잡혔고, 같은 이름표가 화면에서는
    다 보이는데 캡처에서만 `…` 로 잘렸습니다.
    → `index.html` 의 `const FONT` 는 CSS `font-family` 와 **글자 하나까지 같아야** 합니다.

13. **이름표 글자 크기(CSS)와 `pitch_kit.MAX_LABEL_WIDTH` 는 한 쌍이다.**
    한쪽만 바꾸면 이름표가 `…` 로 잘리기 시작합니다.
    현재: 이름표 안쪽 폭 84px · 글꼴 10px → 한글 9.1자분, 여유를 둬서 최대 8.6.
    `pitch_kit.display_width()` 의 계수는 눈대중이 아니라 브라우저에서 `measureText` 로
    실측한 값입니다(한글 0.92em / 대문자 0.703 / 숫자 0.575 / 괄호 0.369).

14. **`.card` 에 blur 그림자를 여러 개 걸지 말 것.**
    모양(SVG path)과 단색 칠은 공짜지만, 카드마다 blur 그림자·그라디언트·이미지 파일을
    쓰면 스크롤할 때마다 다시 칠해져서 툭툭 끊깁니다. 유니폼 26벌을 그려도 느려지지
    않는 이유가 이것입니다.

---

## 9. 예시 포트폴리오(`presets.py`) 수정 방법

종목을 추가·교체할 때는 **반드시 실제로 조회되는지 먼저 확인**하세요. 지어낸 티커를 넣으면
사용자가 버튼을 눌렀을 때 "데이터 없음"만 뜹니다.

```python
from data.providers.registry import get_provider
get_provider("US").get_latest_price("SCHD")   # 예외 안 나면 OK
```

- 비중 합계는 **정확히 100%** 여야 합니다 (`tests/test_presets.py`가 검사).
- 기본 보유 한도(11종목)를 넘기면 안 됩니다.
- `tests/test_integration_network.py::test_every_preset_ticker_actually_resolves`가
  상장폐지·티커 변경을 잡아줍니다. 가끔 `pytest -m network`를 돌려보세요.

---

## 9-2. 데이터 소스 구성 (Alpaca + yfinance + FinanceDataReader)

미국 종목은 **두 소스를 섞어** 씁니다. 이유와 배분 규칙은 `data/providers/us_composite.py`
주석에 자세히 있고, 요약하면:

| 호출 | 1순위 | 이유 |
|---|---|---|
| 최신가, 분배금 | **Alpaca** | 화면 그릴 때마다 종목 수만큼 호출되는 잦은 요청. Alpaca 는 **API 키 기준** 할당량이라 여러 명이 써도 안전 (yfinance 는 서버 IP 기준이라 막힐 수 있음) |
| 가격 히스토리, 분할 | **yfinance** | 백테스트에서만 쓰는 드문 요청이지만 **과거가 깊어야 함**. Alpaca 무료 티어는 약 2020년 중반까지만 있어서, 먼저 쓰면 그 이전 백테스트가 막힘 |

어느 쪽이 실패해도 자동으로 반대쪽으로 넘어갑니다(`FallbackProvider`).
**Alpaca 키가 없으면 기존처럼 yfinance 단독으로 동작**하므로, 키 없이도 앱은 그대로 돕니다.

### Alpaca 를 건드릴 때 반드시 알아야 할 것 (실제 응답으로 검증한 내용)

1. **`adjustment=split` 을 써야 합니다.**
   NVDA 2024-06-03 종가: `raw` 1,149.99 / `split` 115.00 / `all` 114.67.
   이 앱은 "가격은 분할 소급, 배당은 별도" 전제입니다. `all` 을 쓰면 배당이 가격에도
   반영되어 **분배금이 이중 계산**됩니다.

2. **날짜 필터가 배당락일(ex_date)이 아니라 처리일(process_date) 기준입니다.** ⚠️
   실제로 여기서 버그가 났었습니다. O(Realty Income)의 ex_date 2026-08-31 배당은
   process_date 가 2026-09-15 라, `end=오늘` 로 조회하면 **빠집니다.**
   그대로 두면 **월배당 종목의 가장 최근 1건이 매번 누락**되어 분배금이 낮게 나옵니다
   (O 기준 약 8% 과소). 그래서 조회 구간을 미래로 120일 늘려 받고,
   ex_date 기준 자르기는 우리 코드에서 직접 합니다.
   `tests/test_alpaca_provider.py` 가 이걸 지킵니다.

3. **분할 비율은 `new_rate / old_rate`** (10:1 분할 → 10.0, yfinance 와 같은 표현).

4. **한국 종목은 없습니다.** `329200` 등은 HTTP 400 `invalid symbol`.

5. 무료 티어는 **IEX 체결 기준**이라 통합시세(SIP)와 0.1% 안팎 차이가 납니다
   (실측: SCHD/JEPQ/O 최대 0.2%, 평균 0.03%). 포트폴리오 구성에는 충분합니다.

### 키 설정
`.streamlit/secrets.toml` (gitignore 되어 있음):
```toml
ALPACA_API_KEY = "..."
ALPACA_SECRET_KEY = "..."
ADMIN_KEY = "..."        # 선택: 캐시 비우기 버튼을 관리자만 보이게
```
배포(Streamlit Cloud) 시에는 같은 내용을 그쪽 Secrets 칸에 붙여넣습니다.

## 9-3. 관리자 전용 기능

`🔄 데이터 업데이트`(캐시 비우기)는 **모든 접속자가 공유하는 캐시**를 지우기 때문에,
아무나 누르면 매번 전체 시세를 다시 받아와 요청 한도에 걸립니다.
그래서 `app.is_admin()` 으로 가립니다.

- `ADMIN_KEY` 를 설정하지 않으면 → 항상 보임 (개인 PC 용)
- 설정하면 → 주소 뒤에 `?admin=<키>` 를 붙였을 때만 보임

로그인 기능이 아니라 "남들이 실수로 누르지 못하게" 하는 가림막입니다.

## 9-4. 방문자 수 (TODAY / TOTAL)

`services/visitor_service.py`. 제목 오른쪽 위에 표시됩니다.

- **왜 외부 서비스(abacus)인가**: 배포 서버는 앱이 잠들거나 재배포되면 파일이 초기화됩니다.
  서버에 숫자를 적어두면 며칠마다 0 이 되므로 숫자만 외부에 맡깁니다.
- **세션당 1회만** 셉니다. 화면이 다시 그려질 때마다 올리면 숫자가 엉터리가 되므로,
  `app.py` 가 `st.session_state["visitor_counts"]` 로 한 번만 호출되게 막습니다.
- **실패해도 앱은 그대로 동작합니다.** 카운터가 죽으면 숫자만 안 보이고 끝입니다
  (모든 예외를 삼키고 None 반환, 타임아웃 2.5초).
- **정확한 수치가 필요하면** Streamlit Cloud 관리 화면의 Analytics 를 보세요.
  이 카운터는 새로고침·다른 기기를 각각 세므로 "대략 얼마나 들어오는지" 용도입니다.
- **`COUNTER_NS` 를 secrets 에 넣으세요.** 안 넣으면 공개 저장소에 적힌 기본 이름이 쓰여서,
  남이 주소를 알아내 숫자를 부풀릴 수 있습니다. 이름을 바꾸면 0 부터 다시 셉니다.

## 9-5. 시간대 (배포 시 주의)

화면에 `KST` 라고 적으므로 **반드시 `config.now_local()` / `config.today_local()`** 을 쓰세요.
`datetime.now()` / `date.today()` 를 그냥 쓰면 **서버 시간**이 나오는데, Streamlit Cloud 서버는
**UTC** 라서 9시간 어긋난 값이 KST 로 표시됩니다. (실제로 배포 후 발견된 버그)
방문자 "오늘" 집계도 같은 이유로 한국 날짜를 씁니다.

## 9-6. 한국 ETF 이름 줄이기 (`pitch_kit.py`)

**종목 사전이 아니라 "단어 사전"입니다.** 국내 ETF 는 900개가 넘고 매달 새로 나와서
종목을 하나씩 적어두는 방식은 유지가 불가능합니다. 대신 한국 ETF 이름이 늘 같은
부품(`[브랜드] + [기초자산] + [전략] + [꼬리표]`)으로 조립된다는 점을 이용합니다.

| 하고 싶은 일 | 고칠 곳 |
|---|---|
| 새 운용사 유니폼 색 추가 | `BRAND_KITS` 에 한 줄 |
| 리브랜딩(옛 이름 → 새 이름) | `BRAND_ALIASES` 에 한 줄 |
| 새 단어를 짧게 | `REPLACEMENTS` 에 한 줄 (**긴 것부터** 매칭됨) |
| 군더더기 단어 삭제 | `DROP_PATTERNS` 에 한 줄 |

⚠️ **`KEEP_TOKENS` 는 함부로 건드리지 마세요.** `레버리지` `인버스` `2X` `(H)` `TR`
`ATM` `OTM` 은 지우면 상품의 위험도나 성격이 달라져서, 사용자가 잘못 사면 손해로
이어집니다. 글자 수를 넘기더라도 끝까지 남깁니다.

자르는 자리도 규칙이 있습니다(`_protected_spans`). 치환으로 만든 짧은 단어와 괄호
묶음 한가운데서는 자르지 않습니다. 안 그러면 `200고배당커ATM`(`커콜`이 반토막),
`종합채권(AA-이`(괄호 안 닫힘), `코스닥1레버리지`(없는 지수처럼 보임) 같은 게 나옵니다.

모르는 단어가 나와도 망가지지 않습니다 — 치환을 건너뛰고 앞부분만 보여줄 뿐이라
최악이어도 예전(그냥 잘림)보다 나빠지지 않습니다. 그래도 어색하면 사용자가
**상세 패널의 "전술판 이름표"** 에서 직접 고칠 수 있고, 그 값은 `Security.card_label`
로 전술 JSON 에 저장됩니다. 정식 이름은 항상 상세 패널과 캡처 명단에 남습니다.

## 10. 알려진 한계 / 안 하기로 한 것들

- 수수료·세금·환전 스프레드·슬리피지 미반영 (백테스트는 단순 시뮬레이션)
- 백테스트는 Buy & Hold만. 리밸런싱·추가매수·분배금 재투자 없음
- 분배금은 최근 12개월 실적 기준 단순 예상이며 미래를 보장하지 않음
- 상장 12개월 미만 종목은 **실제 2회 이상 지급**된 경우만 연환산 추정하고,
  추정임을 경고로 명시함 (지급 횟수와 환산 방법을 같이 표시)
- 백테스트 시작일보다 늦게 상장한 종목이 있으면 실행 거부
  (없는 과거를 지어내지 않기 위함. 대신 "언제 이후로 잡으면 되는지" 안내함)
- 한국 종목의 야후 파이낸스 링크는 `.KS/.KQ`를 확정할 수 없어 검색 페이지로 보냄
  (토스증권 링크는 `A`+종목코드로 정확히 열림)

---

## 11. 자주 하게 될 작업 요약

**글자 크기/색을 바꾸고 싶다** → `ui_theme.py`의 `INK_*` 상수와 `inject()` 안의 CSS.
Streamlit 내부 클래스명은 버전마다 바뀌므로 **`data-testid`로 타겟팅**할 것.

**전술판 크기를 바꾸고 싶다** → `config.PITCH_MID_COL_RATIO`(가로 폭),
`config.PITCH_ASPECT_RATIO`(세로 길이). 현재 값은 사용자가 27인치 모니터에서 직접 맞춘 값입니다.

**문구를 바꾸고 싶다** → `app.py`에서 해당 문자열. 단, **AppTest가 라벨 문자열로 위젯을 찾으므로
`tests/`도 같이 고쳐야 합니다.** (`pytest`를 돌리면 바로 잡힙니다)

**새 기능을 넣고 싶다** → 계산은 `services/`에 순수 함수로, 화면은 `app.py`에.
그리고 오프라인 테스트를 같이 추가하세요.
