# 무료 배포 가이드 — URL 하나로 공유하기

목표: 커뮤니티에 **주소 하나만 올리면 누구나 딸깍 눌러 바로 쓰는** 상태.

## 결론부터: Streamlit Community Cloud

| 후보 | 왜/왜 안 | 판정 |
|---|---|---|
| **Streamlit Community Cloud** | Streamlit 앱 전용 무료 호스팅. GitHub 연결하면 끝. `https://이름.streamlit.app` | ✅ **이걸로** |
| GitHub Pages | 정적 파일만 가능. 파이썬 실행 불가 | ❌ |
| Hugging Face Spaces | 무료이고 Streamlit 지원. 대안으로 괜찮음 | 🔸 차선책 |
| Render/Railway 등 | 무료 티어가 계속 축소·유료화 중 | 🔸 |

**GitHub Pages로는 안 됩니다.** 이 앱은 파이썬이 서버에서 돌아야 하는데(시세·환율 조회, 계산),
GitHub Pages는 HTML/CSS/JS 같은 정적 파일만 서빙합니다. GitHub는 **코드 저장소 역할**만 하고,
실제 실행은 Streamlit Cloud가 맡는 구조입니다.

---

## 준비 상태 점검 (이미 다 됨)

- [x] `requirements.txt` 있음
- [x] 진입점 `app.py`
- [x] **서버 디스크에 파일을 쓰지 않음** — 전술 저장은 브라우저 다운로드, 불러오기는 업로드
- [x] 캐시는 메모리 방식 (디스크 권한 문제 없음)
- [x] API 키·비밀번호 불필요

---

## 순서

### 1) GitHub에 올리기

```bash
cd "<이 프로젝트 폴더>"
git init
git add .
git commit -m "ETF MANAGER 2027 v1.0.5"
git branch -M main
git remote add origin https://github.com/<아이디>/etf-manager-2027.git
git push -u origin main
```

- 저장소는 **Public**으로 (무료 배포 조건).
- `.gitignore`에 `__pycache__/`, `.pytest_cache/`가 들어있는지 확인하세요.
- **올리기 전에 `git status`로 개인 정보가 섞이지 않았는지 한 번 보세요.**
  `tactics/` 폴더에 본인 포트폴리오 JSON이 있다면 빼는 게 좋습니다(내 보유 종목·금액이 그대로 노출됨).

### 2) Streamlit Cloud에 연결

1. https://share.streamlit.io 접속 → GitHub 계정으로 로그인
2. **New app** → 방금 만든 저장소 선택
3. Branch `main`, Main file path `app.py`
4. (Advanced settings에서 Python 3.12 또는 3.13 선택)
5. Deploy → 2~5분 기다리면 `https://<앱이름>.streamlit.app` 발급

이 주소를 커뮤니티에 올리면 됩니다. 접속자는 설치·로그인 없이 바로 씁니다.

### 3) 업데이트 방법

`git push`만 하면 **자동으로 재배포**됩니다. 따로 할 일 없습니다.

---

## 사람들이 몰릴 때 알아둘 것 (중요)

1. **첫 접속이 느립니다 (30초~1분)**
   한동안 아무도 안 들어오면 앱이 잠들고, 다음 방문자가 깨웁니다.
   글에 "처음 로딩 30초 정도 걸려요"라고 미리 적어두면 이탈이 줄어듭니다.

2. **시세 조회가 막힐 수 있습니다** ⚠️ 가장 큰 위험
   야후 파이낸스는 **서버 IP 기준으로** 요청을 제한합니다. 내 PC에서 혼자 쓸 땐 문제없지만,
   여러 명이 동시에 들어오면 한 IP에서 요청이 몰려 일시적으로 차단될 수 있습니다.
   그러면 "데이터 없음"이 뜹니다.
   - 완화책: 캐시 시간(`config.py`의 `CACHE_TTL_*`)을 늘리세요.
     예: 최신가 15분 → 60분, 가격 히스토리 6시간 → 24시간.
   - 반응을 보는 게 목적이라면 일단 올려보고, 문제가 생기면 그때 늘려도 됩니다.

3. **모든 접속자가 캐시를 공유합니다**
   같은 종목을 여러 명이 보면 한 번만 조회하므로 오히려 유리합니다.
   단, 포트폴리오·입력값은 **접속자별로 완전히 분리**되니 서로 안 섞입니다.

4. **작업 내용은 저장되지 않습니다**
   새로고침하거나 탭을 닫으면 초기화됩니다. "전술 저장(JSON)" 버튼으로 파일을 내려받아야
   나중에 다시 불러올 수 있습니다. 이것도 소개글에 적어두세요.

5. **면책 문구**
   앱 하단에 이미 "투자 판단·추천을 제공하지 않습니다"가 있습니다.
   커뮤니티 글에도 한 줄 넣어두면 불필요한 시비를 줄일 수 있습니다.
